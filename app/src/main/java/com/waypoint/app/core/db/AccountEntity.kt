// declares that this file belongs to the package `com.waypoint.app.core.db`
package com.waypoint.app.core.db

// expression: `data class AccountEntity(`
data class AccountEntity(
    // continues the statement started above: `val id: String,`
    val id: String,
    // continues the statement started above: `val email: String,`
    val email: String,
    // continues the statement started above: `val displayName: String,`
    val displayName: String,
    // continues the statement started above: `val photoUrl: String,`
    val photoUrl: String,
    // continues the statement started above: `val lastLoginMs: Long,`
    val lastLoginMs: Long,
// closes the multi-line argument list started above
)
